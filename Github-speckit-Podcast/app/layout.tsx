import '../styles/globals.css'
import { Inter } from 'next/font/google'
import Header from '@/components/Header'
import Footer from '@/components/Footer'

const inter = Inter({ subsets: ['latin'] })

export const metadata = {
  title: 'PodCast Pro - Modern Podcast Experience',
  description: 'A sleek podcast website featuring the latest episodes, insights, and community discussions.',
  keywords: 'podcast, audio, technology, interviews, insights',
  authors: [{ name: 'PodCast Pro Team' }],
  viewport: 'width=device-width, initial-scale=1',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <Header />
        <main>{children}</main>
        <Footer />
      </body>
    </html>
  )
}